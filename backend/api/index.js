const router = require('express').Router();

// Archivo donde re-dirigimos las rutas hacia cada archivo separado
// Example
// const users = require('./users/userRoute')
// router.use('/users', users)

module.exports = router;